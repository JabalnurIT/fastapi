{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "apt-get update && git-lfs --version && git init && git lfs pull https://github.com/JabalnurIT/fastapi.git/info/lfs main && git-lfs --version && uvicorn naturalness_score.api:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }

}
